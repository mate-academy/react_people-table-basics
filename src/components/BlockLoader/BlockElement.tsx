import { Loader } from '../Loader';

export const BlockElement = () => {
  return (
    <div className="block">
      <div className="box table-container">
        <Loader />
      </div>
    </div>
  );
};
