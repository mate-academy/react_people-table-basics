import './NotFoundPage.css';

const NotFoundPage = () => (
  <div>
    <h1 className="page-not-found">Not found page</h1>
    <img
      src="http://surl.li/btpwr"
      alt="not found"
      className="picture-error"
    />
  </div>
);

export default NotFoundPage;
