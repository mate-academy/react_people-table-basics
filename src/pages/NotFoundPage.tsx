// Página 404 — renderizada para qualquer URL que não corresponda às rotas definidas.
// Capturada pela rota "*" no App.tsx.

export const NotFoundPage = () => <h1 className="title">Page not found</h1>;
