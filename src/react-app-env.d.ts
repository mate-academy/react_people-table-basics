// eslint-disable-next-line
/// <reference types="react-scripts" />
interface People {
  born: number,
  died: number,
  fatherName?: string | null,
  motherName?: string | null,
  name: string,
  sex: string,
  slug: string,
}
