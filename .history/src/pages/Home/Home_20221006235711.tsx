import { Route } from 'react-router-dom';

export const Home = () => {
  return (
    <Route
      path="/"
      element={(

        element = {< h1 className="title">Home Page</h1>}

      )}
/>
  );
};
